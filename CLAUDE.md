# AITerminalInterfaceConfigs

## AI mail

- Check `mail/claude/` for messages; protocol in `mail/README.md`.
  Archive messages after acting on them. Send by writing into the
  recipient's folder under `mail/`.
