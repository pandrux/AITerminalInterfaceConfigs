# Force LF line endings in the repo, let Git handle conversion on checkout
* text=auto eol=lf

# Shell scripts must always be LF
*.sh text eol=lf

# Windows scripts keep CRLF
*.ps1 text eol=crlf

# Config files — LF
*.lua text eol=lf
*.kdl text eol=lf
