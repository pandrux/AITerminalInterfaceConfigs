# API keys and secrets — never commit
ai-keys.sh
*.secret
*.key

# Machine-specific overrides
local-overrides.sh

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Node
node_modules/

# Claude Code's account-synced skills cache. It regenerates this under
# ~/.claude/skills/synced/, which resolves into this repo once bootstrap
# symlinks ~/.claude/skills -> skills/. Per-machine, not ours to version.
skills/synced/

# understand-anything plugin output (/understand knowledge graph cache).
# Regenerated on each run; per-machine and quickly stale.
.ua/
